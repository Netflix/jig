// =================== DO NOT EDIT THIS FILE ====================
//  Generated by Modello Velocity from writer-stax.vm
//  template, any modifications will be overwritten.
// ==============================================================
package com.netflix.tools.jig.internal.org.apache.maven.metadata.v4;

import java.io.IOException;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.Reader;
import java.io.Writer;
import java.text.DateFormat;
import java.util.ArrayList;
import java.util.Date;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Properties;
import java.util.Set;
import java.util.function.Function;
import javax.xml.stream.XMLOutputFactory;
import javax.xml.stream.XMLStreamException;
import javax.xml.stream.XMLStreamWriter;
import com.netflix.tools.jig.internal.org.apache.maven.api.annotations.Generated;
import com.netflix.tools.jig.internal.org.apache.maven.api.xml.XmlNode;
import com.netflix.tools.jig.internal.org.apache.maven.api.metadata.Metadata;
import com.netflix.tools.jig.internal.org.apache.maven.api.metadata.Versioning;
import com.netflix.tools.jig.internal.org.apache.maven.api.metadata.Snapshot;
import com.netflix.tools.jig.internal.org.apache.maven.api.metadata.SnapshotVersion;
import com.netflix.tools.jig.internal.org.apache.maven.api.metadata.Plugin;
import com.netflix.tools.jig.internal.org.codehaus.stax2.util.StreamWriterDelegate;

import static javax.xml.XMLConstants.W3C_XML_SCHEMA_INSTANCE_NS_URI;

@Generated
public class MetadataStaxWriter {

      //--------------------------/
     //- Class/Member Variables -/
    //--------------------------/

    /**
     * Default namespace.
     */
    private static final String NAMESPACE = "http://maven.apache.org/METADATA/1.1.0";

    /**
     * Default schemaLocation.
     */
    private static final String SCHEMA_LOCATION = "https://maven.apache.org/xsd/repository-metadata-1.1.0.xsd";

    /**
     * Field namespace.
     */
    private String namespace = NAMESPACE;

    /**
     * Field schemaLocation.
     */
    private String schemaLocation = SCHEMA_LOCATION;

    /**
     * Field fileComment.
     */
    private String fileComment = null;

      //-----------/
     //- Methods -/
    //-----------/

    /**
     * Method setNamespace.
     *
     * @param namespace the namespace to use.
     */
    public void setNamespace(String namespace) {
        this.namespace = Objects.requireNonNull(namespace);
    } //-- void setNamespace(String)

    /**
     * Method setSchemaLocation.
     *
     * @param schemaLocation the schema location to use.
     */
    public void setSchemaLocation(String schemaLocation) {
        this.schemaLocation = Objects.requireNonNull(schemaLocation);
        } //-- void setSchemaLocation(String)

    /**
     * Method setFileComment.
     *
     * @param fileComment a fileComment object.
     */
    public void setFileComment(String fileComment) {
        this.fileComment = fileComment;
    } //-- void setFileComment(String)

    /**
     * Method write.
     *
     * @param writer a writer object
     * @param metadata a Metadata object
     * @throws IOException IOException if any
     */
    public void write(Writer writer, Metadata metadata) throws IOException, XMLStreamException {
        XMLOutputFactory factory = new com.netflix.tools.jig.internal.com.ctc.wstx.stax.WstxOutputFactory();
        factory.setProperty(XMLOutputFactory.IS_REPAIRING_NAMESPACES, false);
        factory.setProperty(com.netflix.tools.jig.internal.com.ctc.wstx.api.WstxOutputProperties.P_USE_DOUBLE_QUOTES_IN_XML_DECL, true);
        factory.setProperty(com.netflix.tools.jig.internal.com.ctc.wstx.api.WstxOutputProperties.P_ADD_SPACE_AFTER_EMPTY_ELEM, true);
        XMLStreamWriter serializer = new IndentingXMLStreamWriter(factory.createXMLStreamWriter(writer));
        serializer.writeStartDocument(metadata.getModelEncoding(), null);
        writeMetadata("metadata", metadata, serializer);
        serializer.writeEndDocument();
    } //-- void write(Writer, Metadata)

    /**
     * Method write.
     *
     * @param stream a stream object
     * @param metadata a Metadata object
     * @throws IOException IOException if any
     */
    public void write(OutputStream stream, Metadata metadata) throws IOException, XMLStreamException {
        XMLOutputFactory factory = new com.netflix.tools.jig.internal.com.ctc.wstx.stax.WstxOutputFactory();
        factory.setProperty(XMLOutputFactory.IS_REPAIRING_NAMESPACES, false);
        factory.setProperty(com.netflix.tools.jig.internal.com.ctc.wstx.api.WstxOutputProperties.P_USE_DOUBLE_QUOTES_IN_XML_DECL, true);
        factory.setProperty(com.netflix.tools.jig.internal.com.ctc.wstx.api.WstxOutputProperties.P_ADD_SPACE_AFTER_EMPTY_ELEM, true);
        XMLStreamWriter serializer = new IndentingXMLStreamWriter(factory.createXMLStreamWriter(stream, metadata.getModelEncoding()));
        serializer.writeStartDocument(metadata.getModelEncoding(), null);
        writeMetadata("metadata", metadata, serializer);
        serializer.writeEndDocument();
    } //-- void write(OutputStream, Metadata)

    private void writeMetadata(String tagName, Metadata metadata, XMLStreamWriter serializer)
        throws IOException, XMLStreamException {
        if (metadata != null) {
            if (this.fileComment != null) {
                serializer.writeCharacters("\n");
                serializer.writeComment(this.fileComment);
                serializer.writeCharacters("\n");
            }
            serializer.writeStartElement("", tagName, namespace);
            serializer.writeNamespace("", namespace);
            serializer.writeNamespace("xsi", W3C_XML_SCHEMA_INSTANCE_NS_URI);
            serializer.writeAttribute(W3C_XML_SCHEMA_INSTANCE_NS_URI, "schemaLocation", namespace + " " + schemaLocation);
            writeAttr("modelVersion", metadata.getModelVersion(), serializer);
            writeTag("groupId", null, metadata.getGroupId(), serializer);
            writeTag("artifactId", null, metadata.getArtifactId(), serializer);
            writeVersioning("versioning", metadata.getVersioning(), serializer);
            writeTag("version", null, metadata.getVersion(), serializer);
            writeList("plugins", false, metadata.getPlugins(), serializer,
                    t -> writePlugin("plugin", t, serializer));
            serializer.writeEndElement();
        }
    }

    private void writeVersioning(String tagName, Versioning versioning, XMLStreamWriter serializer)
        throws IOException, XMLStreamException {
        if (versioning != null) {
            serializer.writeStartElement(namespace, tagName);
            writeTag("latest", null, versioning.getLatest(), serializer);
            writeTag("release", null, versioning.getRelease(), serializer);
            writeList("versions", versioning.getVersions(), serializer,
                    t -> writeTag("version", null, t, serializer));
            writeTag("lastUpdated", null, versioning.getLastUpdated(), serializer);
            writeSnapshot("snapshot", versioning.getSnapshot(), serializer);
            writeList("snapshotVersions", false, versioning.getSnapshotVersions(), serializer,
                    t -> writeSnapshotVersion("snapshotVersion", t, serializer));
            serializer.writeEndElement();
        }
    }

    private void writeSnapshot(String tagName, Snapshot snapshot, XMLStreamWriter serializer)
        throws IOException, XMLStreamException {
        if (snapshot != null) {
            serializer.writeStartElement(namespace, tagName);
            writeTag("timestamp", null, snapshot.getTimestamp(), serializer);
            writeTag("buildNumber", "0", Integer.toString(snapshot.getBuildNumber()), serializer);
            writeTag("localCopy", "false", snapshot.isLocalCopy() ? "true" : null, serializer);
            serializer.writeEndElement();
        }
    }

    private void writeSnapshotVersion(String tagName, SnapshotVersion snapshotVersion, XMLStreamWriter serializer)
        throws IOException, XMLStreamException {
        if (snapshotVersion != null) {
            serializer.writeStartElement(namespace, tagName);
            writeTag("classifier", null, snapshotVersion.getClassifier(), serializer);
            writeTag("extension", null, snapshotVersion.getExtension(), serializer);
            writeTag("value", null, snapshotVersion.getVersion(), serializer);
            writeTag("updated", null, snapshotVersion.getUpdated(), serializer);
            serializer.writeEndElement();
        }
    }

    private void writePlugin(String tagName, Plugin plugin, XMLStreamWriter serializer)
        throws IOException, XMLStreamException {
        if (plugin != null) {
            serializer.writeStartElement(namespace, tagName);
            writeTag("name", null, plugin.getName(), serializer);
            writeTag("prefix", null, plugin.getPrefix(), serializer);
            writeTag("artifactId", null, plugin.getArtifactId(), serializer);
            serializer.writeEndElement();
        }
    }

    @FunctionalInterface
    private interface ElementWriter<T> {
        public void write(T t) throws IOException, XMLStreamException;
    }

    private <T> void writeList(String tagName, List<T> list, XMLStreamWriter serializer, ElementWriter<T> writer) throws IOException, XMLStreamException {
        writeList(tagName, false, list, serializer, writer);
    }

    private <T> void writeList(String tagName, boolean flat, List<T> list, XMLStreamWriter serializer, ElementWriter<T> writer) throws IOException, XMLStreamException {
        if (list != null && !list.isEmpty()) {
            if (!flat) {
                serializer.writeStartElement(namespace, tagName);
            }
            int index = 0;
            for (T t : list) {
                writer.write(t);
            }
            if (!flat) {
                serializer.writeEndElement();
            }
        }
    }

    private <T> void writeProperties(String tagName, Map<String, String> props, XMLStreamWriter serializer) throws IOException, XMLStreamException {
        if (props != null && !props.isEmpty()) {
            serializer.writeStartElement(namespace, tagName);
            for (Map.Entry<String, String> entry : props.entrySet()) {
                String key = entry.getKey();
                writeTag(key, null, entry.getValue(), serializer);
            }
            serializer.writeEndElement();
        }
    }

    private void writeDom(XmlNode dom, XMLStreamWriter serializer) throws IOException, XMLStreamException {
        if (dom != null) {
            serializer.writeStartElement(namespace, dom.name());
            for (Map.Entry<String, String> attr : dom.attributes().entrySet()) {
                if (attr.getKey().startsWith("xml:")) {
                    serializer.writeAttribute("http://www.w3.org/XML/1998/namespace",
                    attr.getKey().substring(4), attr.getValue());
                } else {
                    serializer.writeAttribute(attr.getKey(), attr.getValue());
                }
            }
            for (XmlNode child : dom.children()) {
                writeDom(child, serializer);
            }
            String value = dom.value();
            if (value != null) {
                serializer.writeCharacters(value);
            }
            serializer.writeEndElement();
        }
    }

    private void writeTag(String tagName, String defaultValue, String value, XMLStreamWriter serializer) throws IOException, XMLStreamException {
        if (value != null && !Objects.equals(defaultValue, value)) {
            serializer.writeStartElement(namespace, tagName);
            serializer.writeCharacters(value);
            serializer.writeEndElement();
        }
    }

    private void writeAttr(String attrName, String value, XMLStreamWriter serializer) throws IOException, XMLStreamException {
        if (value != null) {
            serializer.writeAttribute(attrName, value);
        }
    }

    static class IndentingXMLStreamWriter extends StreamWriterDelegate {

        int depth = 0;
        boolean hasChildren = false;

        public IndentingXMLStreamWriter(XMLStreamWriter parent) {
            super(parent);
        }

        @Override
        public void writeEmptyElement(String localName) throws XMLStreamException {
            indent();
            super.writeEmptyElement(localName);
            hasChildren = true;
        }

        @Override
        public void writeEmptyElement(String namespaceURI, String localName) throws XMLStreamException {
            indent();
            super.writeEmptyElement(namespaceURI, localName);
            hasChildren = true;
        }

        @Override
        public void writeEmptyElement(String prefix, String localName, String namespaceURI) throws XMLStreamException {
            indent();
            super.writeEmptyElement(prefix, localName, namespaceURI);
            hasChildren = true;
        }

        @Override
        public void writeStartElement(String localName) throws XMLStreamException {
            indent();
            super.writeStartElement(localName);
            depth++;
            hasChildren = false;
        }

        @Override
        public void writeStartElement(String namespaceURI, String localName) throws XMLStreamException {
            indent();
            super.writeStartElement(namespaceURI, localName);
            depth++;
            hasChildren = false;
        }

        @Override
        public void writeStartElement(String prefix, String localName, String namespaceURI) throws XMLStreamException {
            indent();
            super.writeStartElement(prefix, localName, namespaceURI);
            depth++;
            hasChildren = false;
        }

        @Override
        public void writeEndElement() throws XMLStreamException {
            depth--;
            if (hasChildren) {
                indent();
            }
            super.writeEndElement();
            hasChildren = true;
        }

        private void indent() throws XMLStreamException {
            super.writeCharacters("\n");
            for (int i = 0; i < depth; i++) {
                super.writeCharacters("  ");
            }
        }
    }
}
